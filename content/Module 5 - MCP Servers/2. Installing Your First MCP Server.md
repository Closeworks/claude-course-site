---
tags:
  - skool
  - course
  - claude-code
  - module-5
---
### Config Location

```
~/.claude/settings.json
```

### The Structure

```json
{
  "mcpServers": {
    "server-name": {
      "command": "the command to start it",
      "args": ["arguments"],
      "env": { "API_KEY": "your-key" }
    }
  }
}
```

Save. Restart Claude Code. Ask: *"What MCP tools do you have?"*

## Action Step

1. Install one MCP server. Verify it works.
2. Mark this lesson as **DONE**.
